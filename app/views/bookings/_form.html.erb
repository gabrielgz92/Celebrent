<%= simple_form_for [@celebrity, @booking] do |f| %>
  <h5>Rate: <%= number_to_currency(@celebrity.rate_per_hour) %></h5>
  <%= f.input :duration %>
  <%= f.input :booking_date, as: :string, required: false, input_html: {class: "datepicker"} %>
  <%= f.submit %>
<% end %>
